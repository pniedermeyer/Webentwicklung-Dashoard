/**
 * Anzahl grafen
Auflösung
Zoom
BL
LK
Fallzahen
Ausgewählter tab
view details
UserID **/
export default function sendUserData(
  UserID,
  selectedBL,
  selectedLK,
  selctedCaseOption,
  // mapCenter,
  resolution,
  zoom,
  graphsShown,
  selectedTab,
  viewDetails
) {
  console.log(
    UserID +
      selectedBL +
      selectedLK +
      selctedCaseOption +
      // mapCenter,
      resolution +
      zoom +
      graphsShown +
      selectedTab +
      viewDetails
  )
  //console.log(arguments)
}
